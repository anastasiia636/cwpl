═══════════════════════════════════════════════════════════════════════════════
                    ГАЙД ДЛЯ ПОДГОТОВКИ К СДАЧЕ КУРСОВОЙ
═══════════════════════════════════════════════════════════════════════════════

📁 СТРУКТУРА ПРОЕКТА И РАСПОЛОЖЕНИЕ ФАЙЛОВ
═══════════════════════════════════════════════════════════════════════════════

Корневая папка проекта:
├── index.html              → Страница заставки (авторизация)
├── game.html               → Страница игры (3 уровня)
├── rating.html             → Страница рейтинга
├── styles.css              → Единая таблица стилей для всех страниц
├── Game_Лейкина_Матрешка.js → ОСНОВНОЙ МОДУЛЬ (вся логика игры)
└── img/                    → Папка с изображениями матрешек
    ├── matreshka-red.png
    ├── matreshka-blue.png
    └── matreshka-yellow.png


🔧 КУДА ИДТИ ДЛЯ ИЗМЕНЕНИЙ
═══════════════════════════════════════════════════════════════════════════════

1. ИЗМЕНИТЬ ВНЕШНИЙ ВИД (цвета, шрифты, размеры, анимации):
   → Файл: styles.css
   → Основные секции:
      • .app-header (строки ~34-80) - хедер с волнами
      • .logo (строки ~201-218) - логотип и плашка
      • .matreshka (строки ~591-650) - стили матрешек
      • .modal-* (строки ~680-750) - модальные окна
      • .btn (строки ~450-520) - кнопки

2. ИЗМЕНИТЬ ЛОГИКУ ИГРЫ (баллы, время, жизни, механика уровней):
   → Файл: Game_Лейкина_Матрешка.js
   → Конфигурация уровней (строки 5-9):
      const LEVEL_CONFIG = {
        1: { timeLimit: 60, correctScore: 10, penaltyScore: -5 },
        2: { timeLimit: 50, correctScore: 20, penaltyScore: -10 },
        3: { timeLimit: 40, correctScore: 30, penaltyScore: -15 }
      };

3. ИЗМЕНИТЬ ВОПРОСЫ:
   → Файл: Game_Лейкина_Матрешка.js
   → Банк вопросов (строки 11-174):
      const QUESTIONS = {
        1: [...],  // Вопросы для уровня 1 (не используются, только для структуры)
        2: [...],  // Вопросы для уровня 2 (не используются)
        3: [...]   // 30 вопросов на эрудицию для уровня 3
      };

4. ИЗМЕНИТЬ КОЛИЧЕСТВО ВОПРОСОВ НА УРОВНЕ:
   → Файл: Game_Лейкина_Матрешка.js
   → Функция proceedToNextQuestionOrLevel() (строки ~860-890)
   → Строка ~863: const questionsNeeded = 3 + Math.floor(Math.random() * 3);
      (генерирует от 3 до 5 вопросов)

5. ИЗМЕНИТЬ КОЛИЧЕСТВО ЖИЗНЕЙ:
   → Файл: Game_Лейкина_Матрешка.js
   → Объект state (строки ~175-185):
      state.lives = 3;  // Изменить здесь

6. ИЗМЕНИТЬ ТЕКСТЫ МОДАЛЬНЫХ ОКОН:
   → Файл: Game_Лейкина_Матрешка.js
   → Функции:
      • showMessage() (строки 713-743) - информационные окна
      • showConfirm() (строки 746-792) - окна подтверждения
      • showEndModal() (строки 794-829) - финальное окно

7. ИЗМЕНИТЬ HTML-СТРУКТУРУ СТРАНИЦ:
   → index.html - заставка
   → game.html - страница игры
   → rating.html - страница рейтинга

8. ИЗМЕНИТЬ РАБОТУ С LOCALSTORAGE:
   → Файл: Game_Лейкина_Матрешка.js
   → Функции:
      • loadCurrentPlayer() (строки 203-211) - загрузка игрока
      • saveRatingRecord() (строки 213-243) - сохранение в рейтинг
   → Ключи:
      • 'matreshka_currentPlayer' - текущий игрок
      • 'matreshka_ratingList' - список рейтинга


📚 БАЗОВЫЕ ПРИНЦИПЫ И СИНТАКСИС
═══════════════════════════════════════════════════════════════════════════════

1. МОДУЛЬНАЯ СТРУКТУРА (IIFE - Immediately Invoked Function Expression):
   (function () {
     // Весь код здесь
     // ...
   })();
   
   → Зачем: Изолирует переменные, предотвращает конфликты с глобальной областью
   → Где: Game_Лейкина_Матрешка.js (строки 4-992)

2. ОБЪЕКТ СОСТОЯНИЯ (state):
   const state = {
     playerName: '',
     currentLevel: 1,
     score: 0,
     lives: 3,
     timeLeft: 0,
     timerId: null,
     currentQuestionIndex: 0,
     finishedLevels: 0,
     totalStartTime: 0,
     levelStartTime: 0,
     availableQuestions: []
   };
   
   → Зачем: Хранит все данные игры в одном месте
   → Где: Game_Лейкина_Матрешка.js (строки ~175-185)

3. КОНСТАНТЫ КОНФИГУРАЦИИ:
   const LEVEL_CONFIG = { ... };
   const QUESTIONS = { ... };
   const STORAGE_KEY_RATING = 'matreshka_ratingList';
   
   → Зачем: Централизованное хранение настроек
   → Где: Game_Лейкина_Матрешка.js (строки 5-9, 11-174, ~177)

4. ДИНАМИЧЕСКОЕ СОЗДАНИЕ DOM-ЭЛЕМЕНТОВ:
   const element = document.createElement('div');
   element.className = 'my-class';
   element.textContent = 'Текст';
   container.appendChild(element);
   
   → Зачем: Создание элементов уровней на лету
   → Где: renderLevel1(), renderLevel2(), renderLevel3()

5. ОБРАБОТЧИКИ СОБЫТИЙ:
   element.addEventListener('click', () => { ... });
   element.addEventListener('dblclick', () => { ... });
   element.addEventListener('mouseover', () => { ... });
   element.addEventListener('keydown', (e) => { ... });
   
   → Зачем: Реакция на действия пользователя
   → Где: Везде в Game_Лейкина_Матрешка.js

6. SETINTERVAL ДЛЯ ТАЙМЕРА:
   state.timerId = setInterval(() => {
     state.timeLeft -= 1;
     // обновление отображения
   }, 1000);
   
   → Зачем: Обновление таймера каждую секунду
   → Где: setTimerForLevel() (строки 298-319)

7. LOCALSTORAGE (JSON):
   // Сохранение:
   localStorage.setItem('key', JSON.stringify(data));
   
   // Загрузка:
   const data = JSON.parse(localStorage.getItem('key'));
   
   → Зачем: Сохранение данных между сессиями
   → Где: loadCurrentPlayer(), saveRatingRecord()

8. МОДАЛЬНЫЕ ОКНА (динамическое создание):
   const overlay = document.createElement('div');
   overlay.className = 'modal-overlay';
   const modal = document.createElement('div');
   modal.className = 'modal-window';
   // ... добавление элементов
   document.body.appendChild(overlay);
   
   → Зачем: Кастомные диалоги вместо alert/confirm
   → Где: showMessage(), showConfirm(), showEndModal()

9. ПЕРЕМЕШИВАНИЕ МАССИВА (алгоритм Фишера-Йетса):
   for (let i = array.length - 1; i > 0; i--) {
     const j = Math.floor(Math.random() * (i + 1));
     [array[i], array[j]] = [array[j], array[i]];
   }
   
   → Зачем: Случайный порядок вопросов/матрешек
   → Где: renderLevel1() (строки 423-426), startLevel() для уровня 3 (строки 331-334)

10. SET ДЛЯ УНИКАЛЬНОСТИ:
    const usedCombinations = new Set();
    usedCombinations.add('small-red');
    if (usedCombinations.has('small-red')) { ... }
    
    → Зачем: Проверка уникальности комбинаций
    → Где: renderLevel1() (строки 401-420)


🎮 МЕХАНИКА КАЖДОГО УРОВНЯ
═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
УРОВЕНЬ 1: "Найди матрешку по описанию"
═══════════════════════════════════════════════════════════════════════════════

ОПИСАНИЕ МЕХАНИКИ:
• Игроку показывается 5 матрешек разных размеров (small, medium, big) и цветов (red, blue, yellow)
• Задача: найти матрешку по описанию (например, "КРАСНУЮ и самую БОЛЬШУЮ")
• Взаимодействие: двойной клик (dblclick) по правильной матрешке
• При наведении (mouseover) матрешка подсвечивается
• При правильном ответе: +10 очков, переход к следующему вопросу
• При неправильном: -5 очков, -1 жизнь, -5 секунд времени

ГДЕ РЕАЛИЗОВАНО:
• Функция: renderLevel1(container) 
• Строки: 381-471 в Game_Лейкина_Матрешка.js

КЛЮЧЕВЫЕ МОМЕНТЫ:
1. Генерация уникальной целевой матрешки (строки 391-397):
   - Случайно выбирается размер и цвет
   - Гарантируется уникальность (нет других матрешек с такой же комбинацией)

2. Генерация остальных матрешек (строки 407-420):
   - Используется Set для отслеживания использованных комбинаций
   - Избегаются дубликаты целевой комбинации

3. Перемешивание матрешек (строки 423-426):
   - Алгоритм Фишера-Йетса для случайного порядка

4. Обработчики событий (строки 450-470):
   - mouseover/mouseout - подсветка при наведении
   - dblclick - выбор матрешки
   - Проверка: cfg.size === target.size && cfg.color === target.color

5. Переход к следующему вопросу (строки 462-465):
   - applyAnswerResult(true, 1) - начисление очков
   - setTimeout(600ms) - задержка перед следующим вопросом


═══════════════════════════════════════════════════════════════════════════════
УРОВЕНЬ 2: "Повтори последовательность"
═══════════════════════════════════════════════════════════════════════════════

ОПИСАНИЕ МЕХАНИКИ:
• Игроку показываются 3 матрешки (красная, синяя, жёлтая)
• Игра автоматически показывает последовательность подсвечивающихся матрешек
• Игрок должен повторить последовательность, кликая по матрешкам
• Длина последовательности увеличивается: 3, 4, 5 элементов
• При ошибке даётся ещё одна попытка (последовательность показывается заново)
• При правильном ответе: +20 очков
• При неправильном: -10 очков, -1 жизнь, -5 секунд

ГДЕ РЕАЛИЗОВАНО:
• Функция: renderLevel2(container)
• Строки: 486-597 в Game_Лейкина_Матрешка.js

КЛЮЧЕВЫЕ МОМЕНТЫ:
1. Генерация последовательности (строки 500-507):
   - Длина: 3 + state.currentQuestionIndex (3, 4, 5)
   - Случайные цвета из ['red', 'blue', 'yellow']

2. Функция playSequence() (строки 528-552):
   - Показывает последовательность автоматически
   - Каждая матрешка подсвечивается на 500ms
   - Интервал между подсветками: 800ms
   - После показа: inputEnabled = true (разрешает ввод)

3. Обработка кликов игрока (строки 554-583):
   - handlePlayerClick(color) - проверяет правильность клика
   - playerIndex - индекс текущего шага в последовательности
   - При ошибке: показывается сообщение, даётся повторная попытка
   - При успехе: переход к следующему вопросу

4. Повторная попытка (строки 560-570):
   - При ошибке последовательность показывается заново
   - playerIndex сбрасывается, inputEnabled снова включается


═══════════════════════════════════════════════════════════════════════════════
УРОВЕНЬ 3: "Вопросы на эрудицию"
═══════════════════════════════════════════════════════════════════════════════

ОПИСАНИЕ МЕХАНИКИ:
• Игрок кликает на любую из 3 матрешек (красную, синюю, жёлтую)
• Появляется вопрос из банка из 30 вопросов
• Вопросы выбираются случайно и не повторяются в рамках раунда
• 4 варианта ответа, пронумерованные 1-4
• Выбор ответа: нажатие клавиш 1, 2, 3 или 4 на клавиатуре
• При правильном ответе: +30 очков
• При неправильном: -15 очков, -1 жизнь, -5 секунд

ГДЕ РЕАЛИЗОВАНО:
• Функция: renderLevel3(container)
• Строки: 601-708 в Game_Лейкина_Матрешка.js

КЛЮЧЕВЫЕ МОМЕНТЫ:
1. Инициализация перемешанных вопросов (строки 327-338, 350-361):
   - При старте уровня 3 все 30 вопросов перемешиваются
   - Сохраняются в state.availableQuestions

2. Функция showQuestion() (строки 618-690):
   - Проверяет, есть ли активный вопрос (если есть - не показывает новый)
   - Если вопросы закончились - перемешивает заново
   - Выбирает случайный вопрос из доступных
   - Удаляет использованный вопрос из массива (splice)
   - Создаёт DOM-элементы для вопроса и вариантов ответа

3. Обработчик клавиатуры (строки 661-684):
   - keydown - слушает нажатия клавиш 1, 2, 3, 4
   - e.key проверяет нажатую клавишу
   - Сравнивает с activeQuestion.correctIndex
   - При правильном ответе: applyAnswerResult(true, 3)
   - При неправильном: applyAnswerResult(false, 3)

4. Уникальность вопросов:
   - Вопросы не повторяются пока не закончатся все 30
   - После использования всех вопросов массив перемешивается заново


═══════════════════════════════════════════════════════════════════════════════
ОБЩАЯ МЕХАНИКА ИГРЫ
═══════════════════════════════════════════════════════════════════════════════

1. СИСТЕМА ПРОГРЕССИИ:
   • Функция: proceedToNextQuestionOrLevel()
   • Строки: ~860-890
   • Логика:
     - Увеличивает state.currentQuestionIndex
     - Проверяет, достигнуто ли нужное количество вопросов (3-5)
     - Если да - переход на следующий уровень
     - Если нет - следующий вопрос на том же уровне

2. ТАЙМЕР:
   • Функция: setTimerForLevel(level)
   • Строки: 298-319
   • Логика:
     - Устанавливает время из LEVEL_CONFIG
     - setInterval обновляет каждую секунду
     - При истечении времени - завершение уровня

3. СИСТЕМА ОЧКОВ И ЖИЗНЕЙ:
   • Функция: applyAnswerResult(isCorrect, level)
   • Строки: 831-860
   • Логика:
     - Правильный ответ: +очки из LEVEL_CONFIG.correctScore
     - Неправильный: +штраф из LEVEL_CONFIG.penaltyScore, -1 жизнь, -5 секунд
     - При lives === 0 - завершение игры

4. ЗАВЕРШЕНИЕ ИГРЫ:
   • Функция: finishGame(fromLevels)
   • Строки: ~900-950
   • Логика:
     - Вычисляет общее время прохождения
     - Показывает модальное окно с результатами
     - Сохраняет в рейтинг (если fromLevels === true)
     - Перенаправляет на rating.html

5. СОХРАНЕНИЕ В РЕЙТИНГ:
   • Функция: saveRatingRecord(name, score, totalTimeMs)
   • Строки: 213-243
   • Логика:
     - Загружает существующий рейтинг из localStorage
     - Если игрок уже есть - обновляет только лучший результат
     - Сортирует по убыванию очков, при равных - по времени
     - Сохраняет обратно в localStorage


💡 ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ НА ЗАЩИТЕ
═══════════════════════════════════════════════════════════════════════════════

Q: "Как работает сохранение данных?"
A: Используется localStorage с ключами 'matreshka_currentPlayer' и 'matreshka_ratingList'.
   Данные сериализуются в JSON через JSON.stringify() и десериализуются через JSON.parse().
   Функции: loadCurrentPlayer() и saveRatingRecord().

Q: "Почему используется IIFE?"
A: Для изоляции кода и предотвращения конфликтов с глобальной областью видимости.
   Все переменные и функции находятся внутри модуля, наружу экспортируется только
   объект window.MatreshkaGame с нужными методами.

Q: "Как реализована случайность?"
A: Math.random() для генерации случайных значений. Для перемешивания массивов
   используется алгоритм Фишера-Йетса. Для уникальности комбинаций - Set.

Q: "Почему не используется requestAnimationFrame?"
A: Используется setInterval для таймера, так как нужны обновления раз в секунду,
   а не каждый кадр. Для игр с анимацией можно использовать requestAnimationFrame,
   но для пошаговой игры setInterval более подходит.

Q: "Как обрабатываются события?"
A: Через addEventListener для разных типов событий:
   - dblclick (уровень 1)
   - mouseover/mouseout (уровень 1)
   - click (уровень 2)
   - keydown (уровень 3)

Q: "Как работает система жизней?"
A: Начальное значение: 3 жизни. При неправильном ответе уменьшается на 1.
   Проверка в applyAnswerResult(): если lives === 0, игра завершается.
   Отображается в информационной панели через livesDisplay.

Q: "Как определяется победа/поражение?"
A: Победа: прохождение всех 3 уровней с сохранением хотя бы 1 жизни.
   Поражение: потеря всех жизней, истечение времени, досрочное завершение.
   Определяется в finishGame() через параметр isWin.


🎯 БЫСТРЫЙ СПРАВОЧНИК: ЧТО ГДЕ МЕНЯТЬ
═══════════════════════════════════════════════════════════════════════════════

Время на уровень:        LEVEL_CONFIG (строки 5-9)
Очки за правильный ответ: LEVEL_CONFIG (строки 5-9)
Штраф за ошибку:          LEVEL_CONFIG (строки 5-9)
Количество жизней:        state.lives = 3 (строка ~180)
Количество вопросов:      proceedToNextQuestionOrLevel() (строка ~863)
Вопросы уровня 3:          QUESTIONS[3] (строки 22-174)
Текст модальных окон:      showMessage(), showConfirm(), showEndModal()
Стили матрешек:           styles.css, секция .matreshka
Стили хедера:             styles.css, секция .app-header
Стили кнопок:             styles.css, секция .btn
Цвета темы:              styles.css, CSS-переменные (--color-*)


❓ ВОПРОСЫ ПО ЛОГИКЕ JAVASCRIPT (ЧТО МОГУТ СПРОСИТЬ)
═══════════════════════════════════════════════════════════════════════════════

1. "Объясни, как работает эта строка кода:"
   
   Пример 1: const target = { size: sizes[Math.floor(Math.random() * sizes.length)], ... }
   ОТВЕТ: 
   - Math.random() возвращает число от 0 до 1 (не включая 1)
   - sizes.length - длина массива (например, 3)
   - Math.random() * 3 даёт число от 0 до 3 (не включая 3)
   - Math.floor() округляет вниз до целого (0, 1 или 2)
   - sizes[индекс] берёт элемент массива по индексу
   → Результат: случайный элемент из массива sizes
   → Где: renderLevel1(), строка 395

   Пример 2: [allQuestions[i], allQuestions[j]] = [allQuestions[j], allQuestions[i]]
   ОТВЕТ:
   - Деструктуризация массива для обмена значениями
   - Временная переменная не нужна
   - Эквивалентно: temp = a; a = b; b = temp;
   → Где: перемешивание массива (строки 331-334, 423-426)

   Пример 3: usedCombinations.add(`${target.size}-${target.color}`)
   ОТВЕТ:
   - Template literal (обратные кавычки) для создания строки
   - `${переменная}` подставляет значение переменной
   - Set.add() добавляет уникальное значение
   - Если значение уже есть - не добавится
   → Где: renderLevel1(), строка 405

2. "Почему используется Set, а не массив?"
   ОТВЕТ:
   - Set хранит только уникальные значения
   - has() работает быстрее, чем indexOf() для массивов
   - Не нужно проверять вручную, есть ли элемент
   - Автоматически предотвращает дубликаты
   → Где: renderLevel1(), строка 401

3. "Что делает эта конструкция: (function () { ... })()"
   ОТВЕТ:
   - IIFE (Immediately Invoked Function Expression)
   - Функция создаётся и сразу вызывается
   - Создаёт изолированную область видимости
   - Переменные внутри не попадают в глобальную область
   - Предотвращает конфликты имён
   → Где: весь файл Game_Лейкина_Матрешка.js обёрнут в IIFE

4. "Объясни замыкание (closure) в этом коде:"
   Пример: element.addEventListener('click', () => { ... })
   ОТВЕТ:
   - Стрелочная функция имеет доступ к переменным внешней области
   - Даже после завершения renderLevel1(), обработчик помнит переменные
   - Например, finalTargetIndex доступен внутри обработчика dblclick
   - Это и есть замыкание - функция "замыкает" переменные
   → Где: обработчики событий в renderLevel1(), renderLevel2(), renderLevel3()

5. "Почему используется setTimeout?"
   ОТВЕТ:
   - Асинхронное выполнение кода
   - Не блокирует основной поток
   - Позволяет показать анимацию/эффект перед следующим действием
   - Первый параметр - функция, второй - задержка в миллисекундах
   → Где: 
     • setTimeout(() => proceedToNextQuestionOrLevel(), 600) - задержка перед следующим вопросом
     • setTimeout(() => playSequence(), 1000) - повторная попытка на уровне 2

6. "Что делает clearInterval?"
   ОТВЕТ:
   - Останавливает интервал, созданный setInterval
   - Предотвращает утечки памяти
   - Нужно вызывать перед созданием нового интервала
   - Принимает ID интервала (возвращённый setInterval)
   → Где: setTimerForLevel(), строка 299

7. "Объясни работу с localStorage:"
   ОТВЕТ:
   - localStorage.setItem('key', value) - сохраняет строку
   - localStorage.getItem('key') - получает строку или null
   - JSON.stringify() - преобразует объект в строку JSON
   - JSON.parse() - преобразует строку JSON в объект
   - Данные сохраняются между сессиями браузера
   → Где: loadCurrentPlayer(), saveRatingRecord()

8. "Что такое деструктуризация?"
   ОТВЕТ:
   - Извлечение значений из объектов/массивов в переменные
   - Пример: const { name, score } = player;
   - Упрощает код, делает его читабельнее
   → Где: используется в различных местах кода

9. "Почему используется dataset?"
   ОТВЕТ:
   - dataset - доступ к data-атрибутам HTML
   - element.dataset.size читает data-size="..."
   - Безопаснее, чем getAttribute()
   - Автоматически преобразует kebab-case в camelCase
   → Где: renderLevel1(), строка 449

10. "Что делает splice?"
    ОТВЕТ:
    - Удаляет элементы из массива и может вставлять новые
    - array.splice(index, count) - удаляет count элементов начиная с index
    - Возвращает массив удалённых элементов
    - Изменяет исходный массив
    → Где: renderLevel3(), строка 635 - удаление использованного вопроса


🔔 ПРИМЕРЫ ALERT'ОВ, КОТОРЫЕ МОГУТ ПОПРОСИТЬ ДОБАВИТЬ
═══════════════════════════════════════════════════════════════════════════════

1. ALERT ПРИ НАЧАЛЕ УРОВНЯ:
   Где добавить: функция startLevel() или renderCurrentLevel()
   Код:
   alert(`Начало уровня ${level}! Время: ${LEVEL_CONFIG[level].timeLimit} секунд`);
   
   Или более информативно:
   alert(`Уровень ${level}\nВремя: ${LEVEL_CONFIG[level].timeLimit}с\nОчки за правильный ответ: ${LEVEL_CONFIG[level].correctScore}\nШтраф за ошибку: ${LEVEL_CONFIG[level].penaltyScore}`);

2. ALERT ПРИ ПРАВИЛЬНОМ ОТВЕТЕ:
   Где добавить: функция applyAnswerResult(), в начало функции
   Код:
   if (isCorrect) {
     alert(`Верно! +${LEVEL_CONFIG[level].correctScore} очков`);
   }
   
   Или в handleLevel1MatreshkaOpen():
   if (isCorrect) {
     alert('Правильно! Матрешка найдена!');
   }

3. ALERT ПРИ НЕПРАВИЛЬНОМ ОТВЕТЕ:
   Где добавить: функция applyAnswerResult()
   Код:
   if (!isCorrect) {
     alert(`Неправильно! -${Math.abs(LEVEL_CONFIG[level].penaltyScore)} очков, -1 жизнь`);
   }

4. ALERT ПРИ ПОТЕРЕ ЖИЗНИ:
   Где добавить: функция applyAnswerResult(), после state.lives -= 1
   Код:
   if (state.lives === 0) {
     alert('Жизни закончились! Игра окончена.');
   } else {
     alert(`Осталось жизней: ${state.lives}`);
   }

5. ALERT ПРИ ПЕРЕХОДЕ НА СЛЕДУЮЩИЙ УРОВЕНЬ:
   Где добавить: функция proceedToNextQuestionOrLevel(), когда уровень завершён
   Код:
   if (state.currentLevel < 3) {
     alert(`Уровень ${state.currentLevel} пройден! Переход на уровень ${state.currentLevel + 1}`);
   } else {
     alert('Все уровни пройдены! Победа!');
   }

6. ALERT ПРИ ИСТЕЧЕНИИ ВРЕМЕНИ:
   Где добавить: функция setTimerForLevel(), когда timeLeft < 0
   Код:
   alert('Время вышло! Уровень завершён.');

7. ALERT ПРИ КЛИКЕ ПО МАТРЕШКЕ (уровень 1):
   Где добавить: обработчик dblclick в renderLevel1()
   Код:
   m.addEventListener('dblclick', () => {
     alert(`Выбрана матрешка: размер ${cfg.size}, цвет ${cfg.color}`);
     const isCorrect = index === finalTargetIndex;
     handleLevel1MatreshkaOpen(isCorrect, m);
   });

8. ALERT ПРИ НАВЕДЕНИИ НА МАТРЕШКУ:
   Где добавить: обработчик mouseover в renderLevel1()
   Код:
   m.addEventListener('mouseover', () => {
     alert(`Матрешка: ${cfg.size}, ${cfg.color}`);
     m.classList.add('hint');
   });

9. ALERT ПРИ ПОКАЗЕ ПОСЛЕДОВАТЕЛЬНОСТИ (уровень 2):
   Где добавить: функция playSequence()
   Код:
   alert(`Запомните последовательность из ${sequence.length} элементов!`);
   
   Или после показа:
   alert(`Повторите последовательность: ${sequence.join(', ')}`);

10. ALERT ПРИ ВЫБОРЕ ВОПРОСА (уровень 3):
    Где добавить: функция showQuestion()
    Код:
    alert(`Вопрос: ${activeQuestion.text}\nВыберите ответ клавишами 1-4`);

11. ALERT ПРИ НАЖАТИИ КЛАВИШИ (уровень 3):
    Где добавить: обработчик keydown в renderLevel3()
    Код:
    alert(`Выбран ответ ${keyNum} (${activeQuestion.options[keyNum - 1]})`);

12. ALERT С ТЕКУЩИМ СОСТОЯНИЕМ ИГРЫ:
    Где добавить: можно добавить кнопку "Показать состояние" или в любой момент
    Код:
    alert(`Игрок: ${state.playerName}\nУровень: ${state.currentLevel}\nОчки: ${state.score}\nЖизни: ${state.lives}\nВремя: ${state.timeLeft}с`);

13. ALERT ПРИ СОХРАНЕНИИ В РЕЙТИНГ:
    Где добавить: функция saveRatingRecord()
    Код:
    alert(`Результат сохранён в рейтинг!\nИмя: ${name}\nОчки: ${score}\nВремя: ${formatDuration(totalTimeMs)}`);

14. ALERT ПРИ ЗАГРУЗКЕ ИГРОКА:
    Где добавить: функция initGamePage()
    Код:
    alert(`Добро пожаловать, ${state.playerName}!\nНачало игры с уровня ${state.currentLevel}`);


💡 КАК ОБЪЯСНЯТЬ ЛОГИКУ КОДА НА ЗАЩИТЕ
═══════════════════════════════════════════════════════════════════════════════

1. НАЧНИ С ОБЩЕГО:
   "В этом коде используется [концепция] для [цель]"

2. ОПИШИ ПОШАГОВО:
   "Сначала происходит [шаг 1], затем [шаг 2], в результате [результат]"

3. УКАЖИ МЕСТО В КОДЕ:
   "Это реализовано в функции [название] на строках [номера]"

4. ОБЪЯСНИ ЗАЧЕМ:
   "Это нужно для [причина], потому что [обоснование]"

5. ПРИВЕДИ АЛЬТЕРНАТИВУ:
   "Можно было бы использовать [альтернатива], но [почему выбрано это]"

ПРИМЕР ОТВЕТА:
"В этом коде используется Set для отслеживания уникальных комбинаций матрешек.
Сначала создаётся пустой Set, затем при генерации каждой матрешки проверяется,
есть ли уже такая комбинация через метод has(). Если нет - добавляется через add().
Это реализовано в функции renderLevel1() на строках 401-420.
Это нужно для гарантии уникальности целевой матрешки, чтобы не было неоднозначности
при выборе. Можно было бы использовать массив и indexOf(), но Set работает быстрее
и код получается чище."


🎯 ТИПИЧНЫЕ ЗАДАНИЯ НА ЗАЩИТЕ
═══════════════════════════════════════════════════════════════════════════════

1. "Добавь alert при [событии]"
   → Найти обработчик события
   → Добавить alert() в начало или конец обработчика
   → Показать релевантную информацию

2. "Измени [параметр] на [значение]"
   → Найти константу/переменную в LEVEL_CONFIG или state
   → Изменить значение
   → Объяснить, что изменится в игре

3. "Объясни, как работает [функция]"
   → Назвать функцию
   → Описать входные параметры
   → Описать шаги выполнения
   → Описать возвращаемое значение/эффект

4. "Почему используется [конструкция]?"
   → Назвать альтернативы
   → Объяснить преимущества выбранного подхода
   → Привести пример из кода

5. "Что произойдёт, если [изменение]?"
   → Проследить цепочку вызовов функций
   → Объяснить влияние на другие части кода
   → Указать возможные побочные эффекты


═══════════════════════════════════════════════════════════════════════════════
                            УДАЧИ НА ЗАЩИТЕ! 🍀
═══════════════════════════════════════════════════════════════════════════════

